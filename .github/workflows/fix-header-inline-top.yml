name: Fix header inline top

on:
  push:
    branches: [main]

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replace only inline header top override
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text()
          old = 'position:"fixed",top:"12px",left:"50%",transform:"translateX(-50%)"'
          new = 'position:"fixed",top:"0px",left:"50%",transform:"translateX(-50%)"'
          if s.count(old) != 1:
              raise SystemExit(f'Expected exactly 1 inline top override, found {s.count(old)}')
          p.write_text(s.replace(old, new, 1))
          PY
      - name: Commit fix and remove temporary workflow
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'UI: Remove inline 12px header top override'
          git push
          rm .github/workflows/fix-header-inline-top.yml
          git add .github/workflows/fix-header-inline-top.yml
          git commit -m 'Remove temporary header top fix workflow'
          git push
